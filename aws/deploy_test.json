{
    "jobName": "staging-deploy",
    "jobQueue": "staging-deploy",
    "jobDefinition": "staging-deploy",
    "containerOverrides": {
        "environment": [
            {
                "name": "CMD",
                "value": "test-histogram"
            },
            {
                "name": "DIST_URL",
                "value": "foo"
            }
        ]
    }
}
